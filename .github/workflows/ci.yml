name: ci

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    uses: justfortytwo/.github/.github/workflows/node-ci.yml@main
    # leaf package — no @justfortytwo siblings to link.
